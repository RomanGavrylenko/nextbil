import styled from 'styled-components';
import { colors } from '@/styles/colors';

export const SignupContainer = styled.div`
    flex-basis: 400px;
    padding: 29px 28px 52px 29px;
    background: ${colors.white};
    border-radius: 31px;
`;
